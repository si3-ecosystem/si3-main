import React from "react";

// Customize it like we can use it in nav and footer as well. Use propos to pass the font size, padding, etc.

const Subscribe = () => {
  return <div>Subscribe</div>;
};

export default Subscribe;
